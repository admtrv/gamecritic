/**
 * Logger decorator package provides set of classes and interfaces
 * for logging within the application. It supports the Decorator pattern
 * to extend the functionality of logging mechanisms dynamically at runtime.
 */
package logger_decorator;
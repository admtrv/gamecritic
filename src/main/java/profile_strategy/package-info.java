/**
 * This package uses Strategy pattern to allow dynamic changing of user
 * interface elements based on the user's role without changing objects that use them.
 * Provides strategy implementations for different user profiles in the application. Each strategy
 * configures user interface according to specific needs and permissions of user role
 * it represents.
 */
package profile_strategy;
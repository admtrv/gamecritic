/**
 * Provides classes and interfaces to enforce input validation across different fields
 * within the application. This package implements Factory pattern to manage creation
 * of specific validation rules for different field types.
 */
package validation_factory;